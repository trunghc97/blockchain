---
apiVersion: v1
kind: Service
metadata:
  name: peer-main-bank
  namespace: blockchain-production
  labels:
    app.kubernetes.io/name: blockchain-peer
    app.kubernetes.io/component: peer
    app.kubernetes.io/part-of: blockchain
    peer-type: main-bank
spec:
  type: ClusterIP
  ports:
  - port: 8082
    targetPort: 8082
    name: http
  - port: 9092
    targetPort: 9092
    name: grpc
  - port: 9094
    targetPort: 9094
    name: metrics
  selector:
    app.kubernetes.io/name: blockchain-peer
    app.kubernetes.io/component: peer
    peer-type: main-bank

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: peer-main-bank
  namespace: blockchain-production
  labels:
    app.kubernetes.io/name: blockchain-peer
    app.kubernetes.io/component: peer
    app.kubernetes.io/part-of: blockchain
    peer-type: main-bank
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: blockchain-peer
      app.kubernetes.io/component: peer
      peer-type: main-bank
  template:
    metadata:
      labels:
        app.kubernetes.io/name: blockchain-peer
        app.kubernetes.io/component: peer
        app.kubernetes.io/part-of: blockchain
        peer-type: main-bank
    spec:
      serviceAccountName: blockchain-service-account
      containers:
      - name: peer-main-bank
        image: blockchain/peer-main-bank:latest  # Replace with your registry
        ports:
        - containerPort: 8082
          name: http
        - containerPort: 9092
          name: grpc
        - containerPort: 9094
          name: metrics
        env:
        # Application configuration
        - name: SERVER_PORT
          value: "8082"
        - name: SPRING_PROFILES_ACTIVE
          valueFrom:
            configMapKeyRef:
              name: blockchain-config
              key: SPRING_PROFILES_ACTIVE
        - name: JAVA_OPTS
          valueFrom:
            configMapKeyRef:
              name: blockchain-config
              key: JAVA_OPTS
        # Peer identification
        - name: PEER_ID
          value: "peer-main-bank"
        - name: PEER_TYPE
          value: "MAIN_BANK"
        # MongoDB configuration
        - name: MONGODB_URI
          valueFrom:
            secretKeyRef:
              name: blockchain-secrets
              key: mongodb-main-bank-uri
        # Orderer connection
        - name: ORDERER_ENDPOINTS
          value: "orderer-service.blockchain-production.svc.cluster.local:7050"
        - name: SCF_CHANNEL
          valueFrom:
            configMapKeyRef:
              name: blockchain-config
              key: SCF_CHANNEL
        - name: AUDIT_CHANNEL
          valueFrom:
            configMapKeyRef:
              name: blockchain-config
              key: AUDIT_CHANNEL
        # JWT configuration
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: blockchain-secrets
              key: jwt-secret
        # Monitoring
        - name: MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE
          valueFrom:
            configMapKeyRef:
              name: blockchain-config
              key: MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE
        - name: MANAGEMENT_METRICS_EXPORT_PROMETHEUS_ENABLED
          valueFrom:
            configMapKeyRef:
              name: blockchain-config
              key: MANAGEMENT_METRICS_EXPORT_PROMETHEUS_ENABLED
        volumeMounts:
        - name: peer-data
          mountPath: /app/data
        resources:
          requests:
            cpu: 500m
            memory: 1Gi
          limits:
            cpu: 2000m
            memory: 4Gi
        livenessProbe:
          httpGet:
            path: /health
            port: 8082
          initialDelaySeconds: 60
          periodSeconds: 30
          timeoutSeconds: 5
          failureThreshold: 6
        readinessProbe:
          httpGet:
            path: /health
            port: 8082
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 3
          failureThreshold: 3
      volumes:
      - name: peer-data
        emptyDir: {}  # Use PVC in production

---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: peer-main-bank-hpa
  namespace: blockchain-production
  labels:
    app.kubernetes.io/name: blockchain-peer
    app.kubernetes.io/component: autoscaling
    peer-type: main-bank
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: peer-main-bank
  minReplicas: 1
  maxReplicas: 5
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 80
